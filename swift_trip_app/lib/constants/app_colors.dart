import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF2563EB);
  
  // Text colors
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFFA1A1A1);
  
  // Border colors
  static const Color borderEnabled = Color(0xFFE5E5E5);
  static const Color borderFocused = Color(0xFF2563EB);
  
  // Background colors
  static const Color backgroundLight = Color(0xFFF0F5FF);
  static const Color backgroundGray = Color(0xFFF3F4F6);
  
  // Prevent instantiation
  AppColors._();
}
