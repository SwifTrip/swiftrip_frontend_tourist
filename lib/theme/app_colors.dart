import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color background = Color(0xFF112116); // Deep Dark Green
  static const Color surface = Color(0xFF1E392A);    // Forest Green (Cards)
  
  // Accents
  static const Color accent = Color(0xFFFF5733);     // Sunset Orange
  static const Color accentHover = Color(0xFFE64E2E); // Slightly darker orange
  
  // Text
  static const Color textPrimary = Color(0xFFF5F5DC); // Sand
  static const Color textSecondary = Color(0xFFC2C2B0); // Sand dim (approx)
  
  // Elements
  static const Color border = Color(0xFF2A4D38);      // Lighter green for borders
  static const Color iconDefault = Color(0xFFF5F5DC);
}
